export class psize {

    psizeId: number;
    psizeName: string;
    multiplier: number;

    constructor(psizeId: number, psizeName: string, multiplier: number) {
        this.psizeId = psizeId;
        this.psizeName = psizeName;
        this.multiplier = multiplier;
    }

}